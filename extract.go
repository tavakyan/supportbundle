package supportbundle

// ExtractBundle unzips the bundle archive and if successful
// it returns the path where it unzipped, otherwise an error
func ExtractBundle(filePath string) (string, error) {
	return "", nil
}
